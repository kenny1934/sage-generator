# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret
WORKSPACE_DOMAIN=yourdomain.com

# Gemini API Configuration
GEMINI_API_KEY=AIzaSy...your-gemini-api-key

# Worker Configuration
WORKER_URL=https://sage-oauth-worker.your-subdomain.workers.dev
REDIRECT_URI=https://sage-oauth-worker.your-subdomain.workers.dev/auth/callback

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-random-jwt-secret

# Instructions:
# 1. Copy this file to worker/.dev.vars for local development
# 2. DO NOT commit .dev.vars to git (already in .gitignore)
# 3. For production, set secrets using: wrangler secret put SECRET_NAME
